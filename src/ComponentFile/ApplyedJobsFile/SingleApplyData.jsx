import React from 'react';

const SingleApplyData = ({data}) => {
    const {title} = data
    return (
        <div>
            <h2>title {title}</h2>
            
        </div>
    );
};

export default SingleApplyData;